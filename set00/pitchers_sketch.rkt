;; list-to-pitchers follow template? 
;; def of listrep 
;; pitchers-to-list, not didn't treat first or last special
;; list-to-listrep? return listrep, it is non-empty
;; how def listrep, pitchers, non empty? empty + invariant?


